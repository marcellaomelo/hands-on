module Testes.Test where
--import Fabricante.Role qualified as FabricanteRole
--import Transporte.Role qualified as TransporteRole
--import Hospital.Service qualified as HospitalService
--import Daml.Script

--setup : Script ()
--setup = script do

--operator     <- allocatePartyWithHint "Operator" $ PartyIdHint "Fabricante Anvisa"
--transporte     <- allocatePartyWithHint "Transporte" $ PartyIdHint "Transportadora - SA"
--public     <- allocatePartyWithHint "Public" $ PartyIdHint "Public"
--hospital <- allocatePartyWithHint "Hospital" $ PartyIdHint "Hospital SirioLibanes"

--operatorRoleCid <- submit operator do createCmd FabricanteRole.Role with observers = [public]; ..
--transporteRoleOfferCid <- submit  operator do exerciseCmd operatorRoleCid  FabricanteRole.OfferTransporteRole with provider = transporte
--transporteRoleCid <- submit transporte do exerciseCmd transporteRoleOfferCid TransporteRole.Accept

--hospitalServiceOfferCid <- submit transporte do exerciseCmd transporteRoleCid TransporteRole.OfferHospitalService with customer = hospital  
--hospitalServiceCid <- submit hospital do exerciseCmd hospitalServiceOfferCid HospitalService.Accept

--submit transporte  do exerciseCmd transporteRoleCid TransporteRole.CreatePaciente with hospital;  name = "Anfotericina B" ; lote = "010101"

--pure()

