module Transportadora.Role where
--import Paciente.Model qualified as PacienteModel
--import DA.Optional

template Role
    with        
         provider: Party
         observers : [Party]
      where
         signatory provider
         observer observers

